<Page
    x:Class="App21092015.Views.ListaCarros"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App21092015.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView Name="ltwCarros" FontSize="30" IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Name="tbkID" Text="{Binding CarroID}" FontSize="30" />
                        <TextBlock Name="tbkModelo" Text="{Binding Modelo}" FontSize="30" />
                        <TextBlock Name="tbkAno" Text="{Binding Ano}" FontSize="30" />
                    </StackPanel>
            </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
